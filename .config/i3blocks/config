# i3blocks config file
#
# Please see man i3blocks for a complete reference!
# The man page is also hosted at http://vivien.github.io/i3blocks
#
# List of valid properties:
#
# align
# color
# command
# full_text
# instance
# interval
# label
# min_width
# name
# separator
# separator_block_width
# short_text
# signal
# urgent

# Global properties
#
# The top properties below are applied to every block, but can be overridden.
# Each block command defaults to the script name to avoid boilerplate.
# Change $SCRIPT_DIR to the location of your scripts!
command=/home/ctznfive/.config/i3blocks/$BLOCK_NAME
separator_block_width=15
markup=none

#[moc]
#command=/home/ctznfive/.config/i3blocks/moc.sh
#color=#757575
#interval=5
#separator_block_width=50


#[active window]
#command=xprop -id $(xdotool getactivewindow) | grep 'WM_NAME(STRING)' | cut -d'"' -f2
#interval=1
#separator_block_width=60


# Timer
[timer_and_stopwatch/timer_and_stopwatch]
format=json
interval=1

# POMODORO Timer
#[pomodoro/pomodoro]
#label=  
#command=/home/ctznfive/.config/i3blocks/pomodoro/pomodoro
#interval=1
#separator=false

# CPU usage
#
# The script may be called with -w and -c switches to specify thresholds,
# see the script for details.
[cpu_usage/cpu_usage]
label=  
interval=10
min_width=CPU 100%
separator=false

# Memory usage
#
# The type defaults to "mem" if the instance is not specified.
[memory/memory]
label=  
separator=false
interval=30

#[memory/memory]
#label=SWAP
#instance=swap
#separator=false
#interval=30

# Disk usage
#
# The directory defaults to $HOME if the instance is not specified.
# The script may be called with a optional argument to set the alert
# (defaults to 10 for 10%).
#[disk/disk]
#label=/
#DIR=/
#interval=30

[disk/disk]
label=  
instance=/
interval=60

[disk/disk]
label=  
#DIR=/mnt/data
interval=60

[disk/disk]
label=++  
instance=/mnt/hdd
interval=60

# Network interface monitoring
#
# If the instance is not specified, use the interface used for default route.
# The address can be forced to IPv4 or IPv6 with -4 or -6 switches.
#[iface/iface]
#label=Δ
#IFACE=wlan0
#color=#00FF00
#interval=60
#separator=false

#[wifi]
#INTERFACE=wlp3s0
#label=wifi:
#interval=10
#separator=false

#[bandwidth/bandwidth]
#INTERFACE=eth0
#interval=5



#[load_average/load_average]
#label=LOAD
#interval=10

# Battery indicator
#
# The battery instance defaults to 0.
#[battery/battery]
#command=$SCRIPT_DIR/battery
#label=BAT
#instance=0
#interval=30


##uptime##
#[uptime]
#command=uptime -p
#interval=1



#[battery2]
#command=/home/ctznfive/.config/i3blocks/battery/battery
#label=⚡
#markup=pango
#interval=30

# Volume indicator
#
# The first parameter sets the step (and units to display)
# The second parameter overrides the mixer selection
# See the script for details.
#[volume/volume]
#label=  
#instance=Master
#instance=PCM
#interval=1
#signal=10


#[ssid/ssid]
#INTERFACE=wlp0s20u5
#interval=60


[volume-pulseaudio/volume-pulseaudio]
interval=persist
SUBSCRIBE=1
USE_ALSA_NAME=1



# Brightness
#[script]
#label=#
#command=/home/ctznfive/.config/i3blocks/backlight.sh
#interval=1

# AUR package: xkb-switch-git
[keymap]
#label=  
command=bash -c 'xkb-switch;xkb-switch -W'
interval=persist

#[kbdd_layout]
#label=  
#command=/home/ctznfive/.config/i3blocks/kbdd_layout/kbdd_layout
#interval=persist


#[batterybar/batterybar]
#label=⚡
#interval=30
#markup=pango
#min_width=bat: ■■■■■


# Date Time
#
[time]
#label=   
command=date '+[ %d/%m %a      %H:%M ]    '
align=center
interval=1
color=#6dafde


# Generic media player support
#
# This displays "ARTIST - SONG" if a music is playing.
# Supported players are: spotify, vlc, audacious, xmms2, mplayer, and others.
#[mediaplayer]
#instance=spotify
#interval=5
#signal=10

# OpenVPN support
#
# Support multiple VPN, with colors.
#[openvpn]
#interval=20

# Temperature
#
# Support multiple chips, though lm-sensors.
# The script may be called with -w and -c switches to specify thresholds,
# see the script for details.
#[temperature]
#label=TEMP
#interval=10

# Key indicators
#
# Add the following bindings to i3 config file:
#
# bindsym --release Caps_Lock exec pkill -SIGRTMIN+11 i3blocks
# bindsym --release Num_Lock  exec pkill -SIGRTMIN+11 i3blocks
#[keyindicator]
#KEY=CAPS
#markup=pango
#interval=once
#signal=11

#[keyindicator]
#KEY=NUM
#markup=pango
#interval=once
#signal=11

